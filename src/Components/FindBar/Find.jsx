import { FindBar, FindBarContainer } from "./FindStyles";
export default function Find() {
    return (
        <FindBarContainer>
            <FindBar placeholder="Поиск по сайту"></FindBar>
        </FindBarContainer>
    );
}