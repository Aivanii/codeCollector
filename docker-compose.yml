services:
  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "5000:5000"  # Порт для Vite
    depends_on:
      - backend

  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "8080:8080"  # Порт для Flask
